
## Custom Logo Page — Final Refinement Tweak

Two targeted changes on top of the approved repositioning plan.

---

### 1. Meta Title Tightening

**File:** `src/config/templateConfigs.ts` (line 659)

Change `pageTitle` to:

```
"Custom Logo Signs | Dimensional Metal Signage"
```

Clean, under 60 characters, no "with Depth & Detail."

---

### 2. Navigation Label: "Custom Logos" → "Custom Logo Signs"

**File:** `src/components/Navigation.tsx`
- Desktop dropdown item (line 69): `"Custom Logos"` → `"Custom Logo Signs"`
- Mobile menu link (line 130): `"Custom Logos"` → `"Custom Logo Signs"`

**File:** `src/components/Footer.tsx` (line 64)
- Footer link: `"Custom Logos"` → `"Custom Logo Signs"`

---

### Nothing else changes
- URL stays `/3d-logos`
- Layout, pricing, artisan tone, and all other SEO enhancements from the approved plan remain intact
