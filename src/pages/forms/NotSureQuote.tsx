import { FormPageTemplate } from "@/components/templates/FormPageTemplate";

const NotSureQuote = () => {
  return (
    <FormPageTemplate
      title="Custom Sign Request"
      description="Not sure what type of marquee sign you need? Tell us about your project and we'll help you find the perfect solution."
      formId="12"
    />
  );
};

export default NotSureQuote;
